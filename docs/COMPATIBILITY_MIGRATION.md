# Compatibility and migration

`Pipeline`, `MapResult`, `PipelineConfig`, and legacy CLI arguments remain supported for one release. The registry's
`reaction@legacy-1` is a deprecated compatibility adapter; use the versioned API for new integrations.
